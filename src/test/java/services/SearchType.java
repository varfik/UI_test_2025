package services;

/** Тип элемента, который ищется через поисквую строку:
 * - если введено название видео (VIDEO), переход на страницу видео
 * - если введено название канала (CHANNEL), переход на страницу канала
 */
public enum SearchType {
    VIDEO,
    CHANNEL
}